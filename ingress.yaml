apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: dev
spec:
  rules:
  - host:
    http:
      paths:
      - backend:
          service:
            name: maddou
            port:
              number: 8888
        path: /
        pathType: Prefix
