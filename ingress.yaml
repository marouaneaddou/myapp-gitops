apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  namespace: default
spec:
  rules:
  - host: app.com
    http:
      paths:
      - backend:
          service:
            name: wil42
            port:
              number: 8888
        path: /
        pathType: Prefix
